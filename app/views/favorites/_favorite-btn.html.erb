<% if book.favorited_by?(current_user) %>
  <%= link_to book_favorites_path(book), method: :delete, local: false do %><!--remote: torue ではだめなのか？
    ♥<%= book.favorites.count %>
  <% end %>
<% else %>
  <%= link_to book_favorites_path(book), method: :post, local: false do %>
    ♡<%= book.favorites.count %>
  <% end %>
<% end %>